language: cpp
solution: 8GadgetPackIconChanger.sln
os: windows
env:
  - MSBUILD_PATH="C:\Program Files (x86)\Microsoft Visual Studio\2019\Community\MSBuild\Current\Bin"
install:
  - choco install visualstudio2019community --package-parameters "--add Microsoft.VisualStudio.Component.VC.Tools.x86.x64"
script:
  - export PATH=$MSBUILD_PATH:$PATH
  - call "C:\Program Files (x86)\Microsoft Visual Studio\2019\Community\Auxiliary\Build\vcvars32.bat"
  - MSBuild.exe 8GadgetPackIconChanger.sln -m -p:Configuration=Release -p:Platform=Win32
